In order to run the simulation execute the following commands in the MATLAB console while in the Collocated-CFD folder:

add_path   % This command only needs to be executed once after opening MATLAB

SteamReformer